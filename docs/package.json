{
  "scripts": {
    "dev": "vuepress dev .",
    "build": "vuepress build .",
    "format": "prettier --write '**/*.{js,md,yaml}'",
    "format:check": "prettier --check '**/*.{js,md,yaml}'"
  },
  "devDependencies": {
    "prettier": "^2.0.4",
    "vuepress": "^1.4.1",
    "vuepress-plugin-sitemap": "^2.3.1"
  }
}
